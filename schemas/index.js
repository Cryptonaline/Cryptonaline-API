const { Post } = require('./post');
const { Video } = require('./video');
const { Tweet } = require('./tweet');
const { Story } = require('./story');

module.exports = {
  Post,
  Video,
  Tweet,
  Story,
};
